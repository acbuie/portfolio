---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  pageTitle: string;
  bannerTitle?: string;
}

const { pageTitle } = Astro.props;
---

<BaseLayout pageTitle={pageTitle}>
  <slot />
</BaseLayout>
